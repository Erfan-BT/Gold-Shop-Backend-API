export enum ReturnStatus {
    NOTCHECKED = 'Not Checked',
    APPROVED = 'Approved',
    NOTAPPROVED = 'Not Approved',
    CANCELED = 'Canceled'
}

export enum RefundStatus {
    WAITING = 'Waiting',
    REFUNDED = 'Refunded',
    CANCELED = 'Canceled'
}