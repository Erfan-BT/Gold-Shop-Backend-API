export enum ReturnStatus {
    NOTCHECKED = 'Not Checked',
    APPROVED = 'Approved',
    NOTAPROVED = 'Not Aproved',
    CANCELED = 'Canceled'
}

export enum RefundStatus {
    WAITING = 'Waiting',
    REFUNDED = 'Refunded',
    CANCELED = 'Canceled'
}