export enum SettingType {
    STRING = 'STRING',
    NUMBER = 'NUMBER',
    BOOLEAN = 'BOOLEAN',
    JSON = 'JSON',
}

export enum SettingGroup {
    GENERAL = 'GENERAL',
    CONTACT = 'CONTACT',
    SOCIAL = 'SOCIAL',
}